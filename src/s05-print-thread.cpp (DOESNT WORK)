#include <iostream>
#include <thread>
#include <vector>
#include <array>

auto aFunction(int a) -> void;


auto main() -> int {

std::array<std::thread, 42> arr{};
int j = 1;

int sizeOfArray = arr.size();

/*for(auto& i : arr) {
i = std::thread(aFunction, j);
j++;
//std::cout << "Detaching..." << std::endl;
i.detach();
}*/

for (int i = 0; i < sizeOfArray; i++) {

arr[i] = std::thread(aFunction, j);
std::cout << "Detaching..." << std::endl;
arr[i].detach();
j++;

}

return 0;

}

auto aFunction(int a) -> void {

std::cout << "Hello, " << a << "!" << std::endl;

}
